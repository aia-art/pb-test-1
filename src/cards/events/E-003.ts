// E-003 Prompt Theft
// 3 Credits. Main Phase. If opponent activated a model last turn,
// activate your model copying one of their prompts.
// Target = your model instance ID (engine handles the actual activation).
import type { CardModule } from '../_types';
const card: CardModule = { id: 'E-003', effects: {} }; // complex – handled in engine
export default card;
