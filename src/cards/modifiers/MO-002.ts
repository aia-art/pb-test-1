// MO-002 Anime LoRA
// LoRA. Portrait/Fantasy +1 Quality. Landscape -1 Quality (min 1).
// Cannot attach to Coherent variants (enforced by engine target spec).
import type { CardModule } from '../_types';
const card: CardModule = { id: 'MO-002', effects: {} };
export default card;
